// Definieer in lib/scoring/ (of in een types.ts):

// JointAngle (naam, huidige waarde, target, tolerance).

// Technique (naam, betrokken joints, thresholds).

// ScoreResult (overall score, per-joint feedbacktekst).

// Dit kun je als TypeScript types of JSON-schema uitdenken, zodat je later makkelijk rule-based scoring schrijft.
